
## Development

```
# install deps
yarn

# run the server locally (in prod it's using vercel serverless)
yarn server

# run nextjs server for web development
yarn web

# "preview" deployment
vc

# "prod" deployment
vc --prod
```
